#include "matrix.h"
matrix multiply(const matrix& mat_1,const matrix& mat_2) {
	unsigned int prodRows = mat_1.get_rows();
	unsigned int prodColumns = mat_2.get_columns();
	matrix product(prodRows, prodColumns);
	for (int i = 0; i < prodRows; i++) {
		for (int j = 0; j < prodColumns; j++) {
			
		}
	}

}